manifest {
    name        = 'resource-test'
    description = 'Pipeline with real CPU/memory work for resource reporting verification'
    version     = '0.1.0'
}

process {
    executor = 'local'
}
