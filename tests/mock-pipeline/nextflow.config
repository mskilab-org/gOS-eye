/*
 * Config for mock nf-gos pipeline.
 * Processes run locally within the SLURM allocation
 * (no nested SLURM submissions).
 */

process {
    executor = 'local'
}

manifest {
    name        = 'nf-gos-mock'
    description = 'Mock nf-gos pipeline for web portal demo'
    version     = '0.1.0'
}
